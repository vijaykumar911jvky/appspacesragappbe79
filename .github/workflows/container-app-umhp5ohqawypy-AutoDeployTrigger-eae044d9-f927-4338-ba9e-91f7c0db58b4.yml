name: Trigger auto deployment for container-app-umhp5ohqawypy

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-umhp5ohqawypy-AutoDeployTrigger-eae044d9-f927-4338-ba9e-91f7c0db58b4.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPUMHP5OHQAWYPY_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPUMHP5OHQAWYPY_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPUMHP5OHQAWYPY_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPUMHP5OHQAWYPY_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPUMHP5OHQAWYPY_REGISTRY_PASSWORD }}
          containerAppName: container-app-umhp5ohqawypy
          resourceGroup: appspacesragappbe7986b0
          imageToBuild: default/container-app-umhp5ohqawypy:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


